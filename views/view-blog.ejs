<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Blog - Mind Canvas</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@100..900&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <style>
    * {
      font-family: "Outfit", sans-serif;
      box-sizing: border-box;
    }

    body {
      width: 100%;
      height: 100%;
    }
  </style>

</head>

<body class="bg-gray-900 text-gray-300">

  <header class="bg-gray-800 p-4">
    <div class="container mx-auto flex justify-between items-center">
      <a href="#">
        <h1 class="text-3xl font-light cursor-pointer tracking-wide text-white">Mind Canvas</h1>
      </a>

      <nav>
        <ul class="flex justify-center items-center space-x-6 font-light text-white">
          <li><a href="/user/dashboard" class="hover:text-gray-400">Dashboard</a></li>
          <li><a href="/about" class="hover:text-gray-400">About</a></li>
          <li><a href="/contact" class="hover:text-gray-400">Contact</a></li>
          <form action="/user/logout" method="post">
            <button type="submit" class="py-2 px-5 bg-red-500 rounded-md border-2 border-gray-700">Logout</a>
          </form>
        </ul>
      </nav>

    </div>
  </header>

  <div class="p-10 flex flex-col gap-6">
    <div class="flex gap-6 items-center">
      <img src="<%= blog.image %>" class="rounded-md">
      <div class="flex flex-col gap-4">
        <h2 class="text-2xl">Title : <%= blog.title %>
        </h2>
        <p class="text-xl">Subject : <%= blog.description %>
        </p>
        <p class="flex">Author : <%= username %>
        </p>
      </div>

    </div>

    <div>
      <p class="text-md tracking-wide">
        <%= blog.content %>
      </p>
    </div>
  </div>

  <div class="p-10">
    <h2 class="text-2xl">Comments</h2>
    <div class="mt-4">
      <p class="mb-2 text-md">Add your comments</p>
      <form action="/user/comment/<%= blog._id %>" method="post" class="flex gap-4">
        <input type="text" placeholder="Enter your comment" name="comment"
          class="w-full px-4 py-3 bg-gray-700 text-white outline-none border-0 rounded-md">
        <button type="submit" class="px-7 bg-blue-500 rounded-md">Comment</button>
      </form>
    </div>

    <div class="flex flex-col mt-6 gap-4">
      <% comments.reverse().map((comment) => { %>
      <div class="bg-gray-700 py-4 px-8 rounded-md items-center">
        <h3 class="text-lg">User : <%= comment.createdBy.username %></h3>
        <p>Comment : <%= comment.content %></p>
      </div>
      
      <% }) %>
    </div>
  </div>

</body>

</html>